@model IEnumerable<GundamShop.Models.CAPDO>
<ul>
    @foreach (GundamShop.Models.CAPDO cd in Model)
    {
        <li><a href="@Url.Action("FullShopContent", new {maCD = cd.MaCD,page= 1})">@cd.TenCapDo</a></li>
    }
</ul>
